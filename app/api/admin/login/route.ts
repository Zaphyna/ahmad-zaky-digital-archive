import {NextResponse} from 'next/server';import {db} from '@/lib/db';import {verify,setSession} from '@/lib/auth';export async function POST(r:Request){const f=await r.formData();const e=String(f.get('email')||''),p=String(f.get('password')||'');if(e!==(process.env.ADMIN_EMAIL||'jekboger2@gmail.com'))return new NextResponse('Unauthorized',{status:401});const sql=db();if(!sql)return new NextResponse('DATABASE_URL missing',{status:503});const rows:any[]=await sql`SELECT password_hash FROM users WHERE email=${e} LIMIT 1` as any;if(!rows.length||!verify(p,rows[0].password_hash))return new NextResponse('Unauthorized',{status:401});await setSession();return NextResponse.redirect(new URL('/admin',r.url),303)}